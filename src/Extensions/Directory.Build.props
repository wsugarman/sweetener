<Project>

  <!-- Include common build properties -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <ExtensionPackageVersion>6.0.0</ExtensionPackageVersion>
  </PropertyGroup>

</Project>
